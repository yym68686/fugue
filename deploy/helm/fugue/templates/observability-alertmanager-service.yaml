{{- if .Values.observability.alerts.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-observability-alertmanager" (include "fugue.fullname" .) }}
  labels:
    {{- include "fugue.componentLabels" (dict "root" . "component" "observability-alertmanager") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "fugue.componentSelectorLabels" (dict "root" . "component" "observability-alertmanager") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.observability.alerts.port }}
      targetPort: http
      protocol: TCP
{{- end }}
