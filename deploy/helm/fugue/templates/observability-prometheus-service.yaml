{{- if .Values.observability.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-observability-prometheus" (include "fugue.fullname" .) }}
  labels:
    {{- include "fugue.componentLabels" (dict "root" . "component" "observability-prometheus") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "fugue.componentSelectorLabels" (dict "root" . "component" "observability-prometheus") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.observability.metrics.port }}
      targetPort: http
      protocol: TCP
{{- end }}
