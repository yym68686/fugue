diff --git a/templates/deployment.yaml b/templates/deployment.yaml
--- a/templates/deployment.yaml
+++ b/templates/deployment.yaml
@@ -1,3 +1,8 @@
+{{- $apiOwnership := default "helm" .Values.api.ownership -}}
+{{- if not (has $apiOwnership (list "helm" "declarative")) -}}
+{{- fail "api.ownership must be helm or declarative" -}}
+{{- end -}}
+{{- if eq $apiOwnership "helm" }}
 {{- $apiNodeSelector := .Values.api.nodeSelector -}}
 {{- $apiTolerations := default .Values.tolerations .Values.api.tolerations -}}
 {{- $apiAffinity := default .Values.affinity .Values.api.affinity -}}
@@ -7,6 +12,9 @@ apiVersion: apps/v1
 kind: Deployment
 metadata:
   name: {{ include "fugue.apiDeploymentName" . }}
+  annotations:
+    helm.sh/resource-policy: keep
+    fugue.pro/api-ownership: helm
   labels:
     {{- include "fugue.componentLabels" (dict "root" . "component" "api") | nindent 4 }}
 spec:
@@ -437,3 +445,4 @@ spec:
         {{- with $apiTolerations }}
         {{- toYaml . | nindent 8 }}
         {{- end }}
+{{- end }}
diff --git a/templates/dns-daemonset.yaml b/templates/dns-daemonset.yaml
--- a/templates/dns-daemonset.yaml
+++ b/templates/dns-daemonset.yaml
@@ -1,4 +1,8 @@
-{{- if .Values.dns.enabled }}
+{{- $ownership := default "helm" .Values.dns.ownership -}}
+{{- if not (has $ownership (list "helm" "declarative")) -}}
+{{- fail "dns.ownership must be helm or declarative" -}}
+{{- end -}}
+{{- if and .Values.dns.enabled (eq $ownership "helm") }}
 {{- if not .Values.dns.answerIPs }}
 {{- fail "dns.answerIPs must contain at least one authoritative answer IP when dns.enabled=true" }}
 {{- end }}
diff --git a/templates/dns-group-daemonsets.yaml b/templates/dns-group-daemonsets.yaml
--- a/templates/dns-group-daemonsets.yaml
+++ b/templates/dns-group-daemonsets.yaml
@@ -1,4 +1,8 @@
-{{- if .Values.dns.enabled }}
+{{- $ownership := default "helm" .Values.dns.ownership -}}
+{{- if not (has $ownership (list "helm" "declarative")) -}}
+{{- fail "dns.ownership must be helm or declarative" -}}
+{{- end -}}
+{{- if and .Values.dns.enabled (eq $ownership "helm") }}
 {{- range $group := .Values.dns.groups }}
 {{ $name := required "dns.groups[].name is required" $group.name | lower | replace "_" "-" | trunc 30 | trimSuffix "-" -}}
 {{- $edgeGroupID := required (printf "dns.groups[%s].edgeGroupID is required" $name) $group.edgeGroupID -}}
diff --git a/templates/ssh-front-daemonsets.yaml b/templates/ssh-front-daemonsets.yaml
--- a/templates/ssh-front-daemonsets.yaml
+++ b/templates/ssh-front-daemonsets.yaml
@@ -171,7 +171,11 @@ spec:
 {{- $sshFront := default dict .Values.edge.sshFront -}}
+{{- $ownership := default "helm" $sshFront.ownership -}}
+{{- if not (has $ownership (list "helm" "declarative")) -}}
+{{- fail "edge.sshFront.ownership must be helm or declarative" -}}
+{{- end -}}
 {{- $sshFrontEnabled := true -}}
 {{- if hasKey $sshFront "enabled" }}{{- $sshFrontEnabled = $sshFront.enabled -}}{{- end -}}
-{{- if and .Values.edge.enabled $sshFrontEnabled }}
+{{- if and .Values.edge.enabled $sshFrontEnabled (eq $ownership "helm") }}
 {{- $sshImage := default dict $sshFront.image -}}
 {{- $rootEdgeImage := dict "repository" .Values.edge.image.repository "tag" .Values.edge.image.tag "digest" .Values.edge.image.digest "pullPolicy" .Values.edge.image.pullPolicy -}}
 {{- $rootSSHImage := include "fugue.sparseImageIdentity" (dict "fallback" $rootEdgeImage "override" $sshImage "identityKeysDefineOverride" false) | fromJson -}}
diff --git a/templates/image-cache-daemonset.yaml b/templates/image-cache-daemonset.yaml
--- a/templates/image-cache-daemonset.yaml
+++ b/templates/image-cache-daemonset.yaml
@@ -1,12 +1,20 @@
 {{- if .Values.imageCache.enabled }}
+{{- $ownership := default "helm" .Values.imageCache.ownership -}}
+{{- if not (has $ownership (list "helm" "declarative")) -}}
+{{- fail "imageCache.ownership must be helm or declarative" -}}
+{{- end -}}
+{{- if eq $ownership "helm" }}
 {{- $updateStrategyType := required "imageCache.updateStrategy.type is required" .Values.imageCache.updateStrategy.type -}}
 {{- if not (has $updateStrategyType (list "OnDelete" "RollingUpdate")) -}}
 {{- fail "imageCache.updateStrategy.type must be OnDelete or RollingUpdate" -}}
 {{- end -}}
 apiVersion: apps/v1
 kind: DaemonSet
 metadata:
   name: {{ include "fugue.imageCacheName" . }}
+  annotations:
+    helm.sh/resource-policy: keep
+    fugue.pro/image-cache-ownership: helm
   labels:
     {{- include "fugue.componentLabels" (dict "root" . "component" "image-cache") | nindent 4 }}
     fugue.io/rollout-subsystem: node-local-build-plane
@@ -111,5 +119,6 @@ spec:
       {{- with .Values.imageCache.tolerations }}
       tolerations:
         {{- toYaml . | nindent 8 }}
       {{- end }}
 {{- end }}
+{{- end }}
