# API candidate fact release history repair

The candidate-generation API atom was split from the failed multi-atom push. No rejected API intent was deployed; the verified API LKG remains the release predecessor.
