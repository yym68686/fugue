# Release history repair

The production branch history was restored to the last verified serving atom after several preflight-only intent attempts were rejected. The rejected attempts were never deployed. The recovery branch is retained locally for audit; normal releases continue through the declarative workflow.
